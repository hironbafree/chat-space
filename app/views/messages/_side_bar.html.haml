.side-bar
  .side-header
    .side-header__box
      .side-header__box__menu
        =current_user.name
      .side-header__box__menu__icon
        =link_to new_group_path,class:"contents__btn" do
          =icon('far', 'edit') 
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog', class: 'icon')
  -current_user.groups.each do |group|
    .group
      =link_to '#' do
        .side-body
          =group.name
        .side-body-two
          まだメッセージがありません
